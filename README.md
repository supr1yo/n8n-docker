# n8n-docker

My Docker Compose configuration for running n8n. This setup uses ngrok for tunneling.

Add your details in `.env` and start using the setup. It will always pull the latest version of n8n.